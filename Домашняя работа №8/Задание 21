#include <iostream>


int main() {
    
    int number;
    
    std::cin >> number;
    
    while (number > 0) {
        int digit = number % 10;
        std::cout << digit;
        number /= 10;
    }
    
    std::cout << std::endl;

    return 0;
}
