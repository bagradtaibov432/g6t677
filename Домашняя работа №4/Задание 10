int max_digit = 0;

    while (number > 0)
    {
        int digit = number % 10;

        if (max_digit < digit)
        {
            max_digit = digit;
        }

        number /= 10;
//Таибов Баград Имамудинович НПИбд-03-23
//Напишите программу,которая запрашивает у пользователя число и выводит на экран значение наибольшей цифры в нем
    }
