#include <iostream>
using namespace std;

int main(){
    int M;
    cin >> M;
    float** a = new float*[M];
    for (int i = 0; i < M; i++){
        a[i] = new float [M];
    
    }
    for (int i = 0; i < M; i++){
        for (int j = 0; j < M; j++){
            cin >> a[i][j];
        }
    }
    for (int i = 0; i < M; i++){
        for (int j = 0; j < M - i; j++){
            cout << a[j][i] << ' ';
        }
        for(int j = i + 1; j < M; j++){
        cout << a[M - i - 1] [j] << " ";
        }
    
    }
    for(int i = 0; i < M; i++){
        delete[] a[i];
    }
    delete[] a;
    return 0;
}
/* Таибов Багрд НПИбд-03-23
Дана квадратная матрица A порядка M. Начиная с элемента A1,1, вывести ее
элементы следующим образом («уголками»): все элементы первого столбца; элементы последней строки,
кроме первого (уже выведенного) элемента; оставшиеся элементы второго столбца;
оставшиеся элементы предпоследней строки и т.д.; последним выводится элемент A1,M.*/
